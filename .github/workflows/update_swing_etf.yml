name: ETF Swing (16:30 HKT)

on:
  schedule:
    - cron: "30 8 * * 1-5"   # UTC 08:30 = 北京 16:30 (A股收盘后, 错开A股workflow)
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 拉取最新 (拿到其它市场最新xlsx, 供index重建)
        run: git pull --rebase origin main || true
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - name: 安装依赖
        working-directory: hk_swing_pattern
        run: pip install -r requirements.txt
      - name: 生成 ETF 表格
        working-directory: hk_swing_pattern
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_NAME: ${{ secrets.DB_NAME }}
        run: python run_swing_etf.py
      - name: 同步到 public + 提交 + 部署 Pages
        run: |
          mkdir -p public/hk_swing_pattern
          cp hk_swing_pattern/index.html hk_swing_pattern/HK_Swing_Pattern.xlsx hk_swing_pattern/US_Swing_Pattern.xlsx hk_swing_pattern/A_Swing_Pattern.xlsx hk_swing_pattern/ETF_Swing_Pattern.xlsx public/hk_swing_pattern/ 2>/dev/null || true
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add hk_swing_pattern/index.html hk_swing_pattern/ETF_Swing_Pattern.xlsx hk_swing_pattern/HK_Swing_Pattern.xlsx hk_swing_pattern/US_Swing_Pattern.xlsx hk_swing_pattern/A_Swing_Pattern.xlsx public/hk_swing_pattern/
          if git diff --cached --quiet; then
            echo "无变化"
          else
            git commit -m "auto: 更新 ETF Swing $(date -u +%Y-%m-%d)"
            git pull --rebase origin main && git push
          fi
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with: { path: public }
      - uses: actions/deploy-pages@v4
